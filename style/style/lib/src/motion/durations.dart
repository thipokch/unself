abstract class UnDurations {
  // Duration
  // See https://www.nngroup.com/articles/animation-duration/
  // See https://uxdesign.cc/the-ultimate-guide-to-proper-use-of-animation-in-ux-10bd98614fa9

  static const Duration rapid = Duration(milliseconds: 100);
  static const Duration fast = Duration(milliseconds: 200);
  static const Duration moderate = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 400);
  static const Duration dalayed = Duration(milliseconds: 500);
}
