name: unself_app
description: Flutter app for Unself project.

publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^6.0.1
  provider: ^6.0.5

  # service

  unself_database:
  unself_license:
  unself_monitoring:
  unself_user:

  # style

  unself_component:
  unself_style:

  # feature

  unself_license_detail:
  unself_license_list:
  unself_settings_appearance:
  unself_settings_data:
  unself_settings_list:

dev_dependencies:
  # Code Quality

  unself_analysis:
  dart_code_metrics: ^5.4.0
  dependency_validator: ^3.2.2

  # Code Generation

  build_runner: ^2.3.0
  go_router_builder: ^1.0.15

  # Testing

  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  # mocktail: ^0.3.0

flutter:
  uses-material-design: true
