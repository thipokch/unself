name: unself_file
description: Cross file compatibility support for file_picker and archive.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:

  flutter:
    sdk: flutter

  unself_unpack:
  
  archive: ^3.3.0
  cross_file: ^0.3.3
  file_picker: ^5.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  # mocktail: ^0.3.0 
  unself_analysis: