import 'package:un_model/un_model.dart';

const personSlug = 'person';

const personNode = NodeSpec(
  slugged: personSlug,
  name: 'Person',
  description: 'An aggregation of user identity and contacts across apps.',
  jsonSpecs: [],
);
